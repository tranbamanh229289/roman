#include<iostream>
#include<vector>

using namespace std;

vector<vector<int>> threeSum(vector<int>& nums) {
  for (int i = 0; i < nums.size(); i ++) {
    
  }
}

void run() {
  vector<int> v = {-1, 0, 1, 2, -1, -4};

}